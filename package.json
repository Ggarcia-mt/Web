{
  "name": "campuspoli",
  "version": "1.0.0",
  "private": true,
  "description": "CampusPoli - Plataforma web para evaluaciones asistidas por IA y control de asistencia dual",
  "workspaces": [
    "server",
    "client"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "db:local": "npm run db:local -w server",
    "db:migrate": "npm run db:migrate -w server",
    "db:seed": "npm run db:seed -w server",
    "build": "npm run build -w server && npm run build -w client",
    "start": "npm run start -w server",
    "test": "npm test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
